// We invoke the method logger() on line 7 within the debugIt() function
// which has access to variables that are declared outside of the function
// such as `status` which is why debugging is logged.

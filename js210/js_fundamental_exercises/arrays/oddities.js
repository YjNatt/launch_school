/*
 * The when arrays are compared they are only equal
 * both arrays are the same array because Arrays are Objects. If they only
 * contain same values but not the same array they
 * will return false.
 */
